# pytest-for-page-object

activate virtual env with:

``` source virtualenv/bin/activate```


install relavant packages with: 

``` pip install -r requirements.txt ```

change the path to chromedriver.exe in ```conftest.py```
